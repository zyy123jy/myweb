upstream django {    # server 
    server 132.236.59.214:8011; # for a web port socket (we'll use this first)} 和uwsgi端口一致
# configuration of the server
server {    # the port your site will be served on
    listen      80;    # the domain name it will serve for    server_name .example.com; # substitute your machine's IP address or FQDN
    charset     utf-8; 
    # max upload size
    client_max_body_size 75M;   # adjust to taste
 
